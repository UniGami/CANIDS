"""Branch 2: lightweight Isolation Forest over rate/plausibility features
(e.g. inter-arrival rate per ID, out-of-range value counts). Fit on
normal-only data. Independent of Branch 1 — can be built in parallel once
the preprocessing plumbing (Step 5) lands.

TODO (Step 11 of the implementation plan).
"""

from __future__ import annotations

raise NotImplementedError("isolation_forest.py not yet implemented — see plan Step 11")
