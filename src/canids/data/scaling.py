"""Signal scaling/normalization, applied before windows are cached.

TODO (Step 5 of the implementation plan): for real SynCAN, check/confirm the
dataset's existing normalization before deciding what further scaling is
needed. For the synthetic phase, apply a standard per-signal scaler.
"""

from __future__ import annotations

raise NotImplementedError("scaling.py not yet implemented — see plan Step 5")
